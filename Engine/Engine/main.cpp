//Eric Marquez
//General main function for testing purposes
//will be removed later

#include "Time.h"

using namespace Lime;

    int main(int argc, char** argv)
    {
        //Game3D game("3D Demo");
        //game.GameLoop();

        auto timer = std::shared_ptr<Time>();
        //PhysicsEngine e;
        //std::shared_ptr<Collider> obj1 = std::make_shared<Collider>();
        //e.AddObject(obj1);
        //float previousTime = timer.running.seconds();
        //while (true)
        //{
        //    timer.Update();
        //    float currentInterval = (timer.running.seconds() - previousTime);
        //    if (currentInterval >= 0.0f)
        //    {

        //        e.Update(currentInterval);
        //        std::cout << obj1->m_Position.x << "/" << obj1->m_Position.y << "/" << obj1->m_Position.z << std::endl;
        //        previousTime = timer.running.seconds();
        //    }
        //}

        //

		return 0;
    }